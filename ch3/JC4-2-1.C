#include<stdio.h>
#include<conio.h>
#define pi 3.14

main()
{
      float p;
      int r;
      clrscr();
      printf("enter redius:");
      scanf("%d",&r);
      printf("%.2f * %d = %.2f",p,r,r,p*r*r);
      getch();
}